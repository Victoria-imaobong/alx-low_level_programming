#include "main.h"
#include <stdio.h>
/**
 * main -  a program that prints its name, followed by a new line
 * @argc: number of the arguments typed
 * @argv: array pointing to arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
